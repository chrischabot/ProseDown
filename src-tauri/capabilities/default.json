{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for the Markview main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "window-state:default"
  ]
}